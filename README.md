# rpi-zephyr-sdk
## Install dependencies
### Linux(Ubuntu)
```sh
sudo apt install --no-install-recommends git cmake ninja-build gperf ccache dfu-util device-tree-compiler wget python3-dev python3-pip python3-setuptools python3-tk python3-wheel xz-utils file make gcc gcc-multilib g++-multilib libsdl2-dev libmagic1
```
### Windows
```sh
winget install Kitware.CMake Ninja-build.Ninja oss-winget.gperf python Git.Git oss-winget.dtc GNU.Wget2 7zip.7zip mcmilk.7zip-zstd

※NOTE
"The path to the 7z executable is not recognized. Please add its installation directory (e.g., C:\Program Files\7-Zip-Zstandard) to your system's PATH environment variable."
```
